.widget-list {

    & .title {
        @mixin grid;

        & h2 {
            @mixin cell 9;
        }

        & .buttons {
            @mixin cell 3;
            text-align: right;
        }
    }

    & .header {
        @mixin control-text;
        padding-bottom: 5px;
    }

    & .widget {
        @mixin grid;
        align-items: baseline;
        transition: all 200ms;
        border-bottom: 1px solid #ccc;

        & .id {
            @mixin cell 1;
            padding-bottom: 5px;
        }

        & .updated {
            @mixin cell 3;
            padding-bottom: 5px;
        }

        & .name {
            @mixin cell 4;
            padding-bottom: 5px;
        }

        & .buttons {
            @mixin cell 4;
            text-align: right;
            white-space: nowrap;
        }

        &.selected {
            color: #f66;
        }
    }
}